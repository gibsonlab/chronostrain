# Example: Adversarial input to test error model (`adversarial_test_icml`)

Test scripts for generating the performance plots in workshop paper. (Figure 2)

## Running
Before running, change variable `PROJECT_DIR` of `run.sh` to the basepath of this cloned repository 
(located near the top of the file).
